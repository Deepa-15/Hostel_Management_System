package com.hostel.model.enums;

/**
 * Represents the roles available in the Hostel Management System.
 * Used for role-based access control (RBAC).
 */
public enum Role {
    STUDENT,
    WARDEN,
    ADMIN
}
